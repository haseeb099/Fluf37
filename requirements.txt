# NEXUS AI — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core Framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
websockets==12.0

# ── LLM (direct SDKs; no LangChain in runtime) ───────────────────
anthropic==0.30.0
openai==1.35.0

# ── Data & Schemas ─────────────────────────────────────────────
pydantic>=2.10.0,<3
pydantic-settings>=2.6.0,<3

# ── Memory ─────────────────────────────────────────────────────
# ChromaDB: install requirements-vector.txt when not in demo-only mode (see README)
networkx==3.3

# ── Async & HTTP ───────────────────────────────────────────────
httpx==0.27.0
aiofiles==23.2.1
aiosqlite==0.20.0
# ── Logging & Observability ────────────────────────────────────
structlog==24.2.0
python-json-logger==2.0.7

# ── Utilities ──────────────────────────────────────────────────
python-dotenv==1.0.1
tenacity==8.3.0   # retry logic
cachetools==5.3.3
python-dateutil==2.9.0
orjson>=3.10.7    # fast JSON serialization (wheels for Python 3.13)

# ── Security ───────────────────────────────────────────────────
python-jose[cryptography]==3.3.0  # JWT
# passlib reserved for future password auth (not used in v0.9)
slowapi==0.1.9    # rate limiting

# ── Testing ────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-mock==3.14.0
pytest-cov==5.0.0
hypothesis==6.103.2
httpx==0.27.0     # for TestClient

# ── Dev Tools ──────────────────────────────────────────────────
ruff==0.4.10      # linting + formatting
mypy==1.10.0      # type checking
pre-commit==3.7.1
